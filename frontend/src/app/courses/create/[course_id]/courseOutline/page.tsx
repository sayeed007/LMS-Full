"use client";

// This page is intentionally empty because the CourseOutline component
// is rendered through the layout.tsx when activeTab is "outline"
export default function CourseOutlinePage() {
  return null;
}